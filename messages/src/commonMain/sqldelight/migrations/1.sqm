ALTER TABLE message ADD COLUMN timeline_at INTEGER NOT NULL DEFAULT 0;

UPDATE message
SET timeline_at = created_at_local;

DROP INDEX message_created_at;

CREATE INDEX message_created_at ON message(timeline_at DESC, local_id);
